const initialState = {
  error: null,
  message: null
};

export const teamsReducer = (state = initialState, action) => {
  const { type } = action;
  switch (type) {
    default:
      return state;
  }
};
